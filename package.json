{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "yarn@3.6.1",
  "engines": {
    "node": ">=16.10"
  },
  "name": "run-eat",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "yarn workspaces foreach -pv run dev",
    "build": "yarn workspaces foreach -pv run build"
  },
  "keywords": [],
  "author": "",
  "license": "UNLISENCED",
  "devDependencies": {
    "eslint": "^8.39.0",
    "eslint-plugin-react": "latest",
    "husky": "^8.0.3",
    "lint-staged": "^13.2.2"
  },
  "lint-staged": {
    "packages/**/*.{js,jsx,ts,tsx}": [
      "eslint --ext .js,jsx packages/ --fix"
    ]
  }
}
